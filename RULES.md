# 작업 방식 (공통 규칙)

프로젝트와 무관하게 항상 지킬 것. 코드 수정·커밋·리뷰 진행 방식에 대한 규칙이다.

## 1. 진행 흐름

작업 단위마다 이 순서를 지킨다.

```
A. 작업계획 제시  →  B. 사용자 컨펌  →  C. 구현(코드 수정까지만)
→  D. 사용자가 직접 리뷰·빌드·커밋  →  E. 커밋 결과 검토
```

- **내가 하는 것**: 계획 작성, 코드 수정, 커밋 메시지 초안 제시, 커밋된 결과 검토(정적 리뷰)
- **사용자가 하는 것**: 컨펌, 빌드, 실기기 테스트, `git commit` / `rebase` / `squash`

## 2. 하지 않는 것

- **히스토리를 바꾸는 명령을 실행하지 않는다** — `git commit`, `git rebase`, `git push` 등. 요청받지 않는 한 작업 트리 수정까지만 하고 넘긴다.
- **빌드·컴파일을 자율로 돌리지 않는다.** 시간과 토큰이 크게 든다. 직전에 빌드 요청이 있었더라도 그 이후의 개별 수정에는 별도 요청이 필요하다.
- **설계 방향을 묻는 질문에 파일을 고치지 않는다.** "~하는 게 어때?", "~할까?", "이거 이상하지 않아?"는 의견을 묻는 것이다. 분석과 추천을 주고 **명시적인 적용 지시를 기다린다.** (사용자가 동시에 편집 중이면 충돌이 난다)
- **요청 범위 밖의 정리를 제안하지 않는다.** 미사용 리소스·죽은 코드 등은 발견해도 따로 관리하지 않기로 한 영역이면 언급만 하지 말고 넘어간다.

## 3. 계획 세우는 법

- 큰 기능은 **뼈대(구조·빈 껍데기) 먼저 → 각 부분 하나씩** 순으로 쪼갠다. 쪼갠 단위를 먼저 제시하고 컨펌받는다.
- 계획에는 **확인이 필요한 결정 항목을 분리해서** 적는다. 답에 따라 결과물이 달라지는 것만 묻고, 나머지는 판단해서 진행한다.
- 앞으로 할 작업을 **숫자로 부르지 않는다.** "3단계", "커밋 13" 대신 내용 이름으로 지칭한다 — 숫자 체계가 여러 개 얽히면 서로 어긋난다.

## 4. 구현 후 인계

- 변경 파일과 성격(이번 작업분 / 임시 / squash 대상)을 **표로 정리**한다.
- **커밋 메시지 초안을 함께 준다.**
- 임시 상태로 둔 것, 다음 단계로 미룬 것을 명시한다.

## 5. 커밋 히스토리 스타일

브랜치 히스토리를 **리뷰어가 처음부터 읽는 문서**처럼 관리한다. 배포 전이면 "나중에 고친 흔적"을 남기지 않고 squash로 정리한다.

- **커밋 순서는 아래 레이어부터**: 엔진/데이터 → Manager → Flow → UI → 에러 처리 → 다국어. **각 커밋이 그 시점에 독립적으로 컴파일**되어야 한다.
- **메시지 형식**: `<브랜치주제>: <레이어 prefix>) <요약>` + `- ` 불릿 본문. prefix가 레이어를 드러내고, 본문에는 **왜 그렇게 했는지**를 적는다. 제목에 넣은 내용은 본문에도 불릿으로 있어야 한다.
- **커밋 간 불필요한 diff 금지.** 같은 값(padding, 이름 등)이 커밋 사이에 바뀌었다 말았다 하지 않게 한다. 리스트럭처 커밋에 값 변경이 묻히는 게 특히 나쁘다.
- **주석이 미래를 앞서가지 않게.** 아직 존재하지 않는 개념을 설명하는 주석을 미리 넣지 않는다.
- 반대로 **사용하지 않는 필드/속성은 처음부터 있어도 된다** — 리뷰어 입장에서 처음부터 있는 편이 낫다.
- **특수 룰은 별도 커밋으로** 빼서 의도가 보이게 한다. 일반 구현에 묻으면 리뷰어가 못 읽는다.
- **구조를 미리 만들지 않는다.** 형제가 생길 때 감싼다(상태가 하나뿐일 때 컨테이너 래퍼를 미리 넣지 않음).
- **import 순서는 알파벳 정렬 유지.** 어긋나면 IDE가 나중에 정리하면서 불필요한 diff가 생긴다.

## 6. 리베이스

- 리베이스는 **사용자가 직접, CLI로** 한다. IDE(Android Studio)의 in-memory rebase는 **커밋 메시지를 평탄화하고 충돌 시 파일이 유실**된다.
- 중복 커밋을 가진 브랜치는 `rebase --onto <tip> <boundary>`.
- 리베이스 후 검토를 요청받으면 확인할 것: 커밋별 파일 목록, 메시지 정합성, 커밋 간 값 변경, 미래를 앞서간 주석, 체크리스트 grep, 컴파일 검증(요청 시).

## 7. 코드 구조 선호

- **공통 로직 오염 금지.** 공통 컴포넌트를 특정 기능용으로 상속·확장해 비틀지 않는다. 필요하면 전용 신규 컴포넌트를 만든다.
- **base class 정형화 지양.** 추상 계층을 먼저 세우지 않는다.
- **구체적인 flow는 소비 지점에 배치.** 매니저/공통 객체가 오케스트레이션까지 떠안아 비대해지지 않게 한다.
- **재사용 로직은 LogicDelegate로** 뽑는다.
